{
  "name": "tutur-kbbi-explorer",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "shared",
    "api"
  ],
  "scripts": {
    "dev": "bun scripts/dev.mjs",
    "data:prepare": "bun scripts/prepare-data.mjs",
    "api:build": "bun run --cwd api build",
    "api:benchmark": "bun run --cwd api benchmark",
    "api:test": "bun run --cwd api test",
    "build": "bun scripts/build-all.mjs",
    "lint": "bun scripts/lint.mjs",
    "format:check": "prettier --check package.json .prettierrc.json 'shared/**/*.js' 'api/**/*.js' 'scripts/**/*.mjs' 'README.md' '.github/**/*.yml'",
    "test": "bun test shared/test api/test",
    "clean": "bun scripts/clean-generated.mjs"
  },
  "devDependencies": {
    "prettier": "3.9.6"
  }
}
